# apps/web のスキャフォールドとハーネスの SKIP 解消

Issue #9 の仕様。**`apps/web` を最小構成で立ち上げ、`make lint-web` / `make test-web` が「SKIP」ではなく実際に走る状態にする。**

**画面は作らない。** 画面/UI の受け入れ条件は `docs/specs/work-month-screen-ui.md` ほかに既にあり、本仕様はそれらの実装を含まない。本仕様が定めるのは**足場（scaffold）とハーネスの有効化**の受け入れ条件である。

## 人間が確定した決定

**以下は人間の決定であり、AI の仮置きではない。実装・レビュー工程で再検討しない。** 詳細は各項へ。

| 決定日 | 論点 | 決定 | 本文 |
|---|---|---|---|
| 2026-08-14 | レート制限のパラメータ | **案1** — `src/lib/rate-limit.ts` を本 Issue から**外す**。a〜e は最初の Route Handler を作る Issue でセットで決める | U-1 / AC-8-5 / AC-9 |
| 2026-08-14 | コンポーネントの配置先 | **承認** — `apps/web/src/components/`。`ui_kits/` は採らない。本 Issue では位置を決めるだけでコンポーネントは作らない | AC-1-5 |
| 2026-08-14 | 対象が無い場合の扱い | **承認** — `test-web` / `lint-web` は対象が無ければ SKIP ではなく**失敗**。`lint-tf`（infra）の SKIP は据え置く | AC-4-2 / AC-4-5 |
| 2026-08-17 | Vitest テストの Red を誰が確認したか（AC-3-4 の逸脱の扱い） | **案(a)** — reviewer が実測したミューテーションの Red 証跡を記録して閉じる。**コード・履歴には触れない（`test:` / `feat:` への分割と force-push は行わない）。** AC-3-4 の本文は緩めない | AC-3-7 |
| 2026-08-17 | `apps/web/.next` の生成物で `make scan-secrets` が落ちる件（AC-8-3 の停止条件が作動） | **承認** — `.gitleaks.toml` を新設し、allowlist を **`apps/web/.next/` のパスに限定**して導入する。**`[extend] useDefault = true` を必ず入れる。** ビルド成果物一般・ワイルドカードへ広げない。**`gitleaks git .`（履歴側）は無変更** | AC-11 |
| 2026-08-18 | `paths` の値を書き換えるだけで除外を広げられる件（C-3。AC-12 初版が値の**型**しか縛っていなかった） | **案(a)** — **`paths` の許可値を既定形の文字列 `^apps/web/\.next/` に完全一致で固定する。** 「`\|` を禁じる」等の**禁止パターンの列挙で塞がない**（同型の委譲の3度目になる）。許可する文字列を1つに固定する側で書く | AC-12-2-d / 12-2-d-1 |

## なぜ仕様を起こすか

Issue #9 の本文は「この Issue は `docs/specs/` に対応する仕様を持たない」と述べるが、**その記述は Issue 側の申告であって、仕様の要否の決定ではない**（ADR 0004: 正解は常に `docs/`）。本仕様を起こすのは次の理由による。

1. **`apps/` を変更する。** SDD は「`docs/specs/` に仕様書がない機能は実装しない」と定め、例外は PR の `no-spec` ラベル（`docs/rules/development-process.md`）。本 Issue は `apps/web` の新設に加え、**Makefile・CI・`docs/harness/verification-loop.md`・README を同時に書き換える**。受け入れ条件が無いと、何をもって完了かをテストに落とせない
2. **ハーネス側の仕様には前例がある。** `docs/specs/skills.md` / `orchestrator-entry-hook.md` / `issue-command.md` / `devcontainer-go-module-policy.md` / `security-rules.md` / `notation-rules.md` はいずれも画面ではなくハーネス・インフラの仕様である。SDD は画面に限定されていない
3. **ADR 0007 §5 が「設定は #9 の範囲とする」と明示的に委譲している**（Vitest）。委譲された決定の置き場所が要る
4. **SKIP を外すことはハーネスの検査対象を変える操作である。** 「対象が無いとき SKIP するのか失敗するのか」は verification-loop の中核の論点であり、記録なしに変えない

---

## スコープ

- `apps/web` の最小スキャフォールド（Next.js App Router + TypeScript + Tailwind CSS）
- `package.json` の scripts 契約（Makefile が呼ぶもの）
- テスト基盤 Vitest の導入（ADR 0007 §5 が本 Issue へ委譲したもの）
- ハーネスの SKIP 解消（Makefile / `ci.yml` / `docs/harness/verification-loop.md` / README）
- **レート制限（U-1）の扱いの固定** — 2026-08-14 に人間が**案1**（本 Issue から外す）を決定。本 Issue が持つのは「外した」ことと「次にどこで決めるか」の記録だけであり、レート制限の実装は含まない

### 非スコープ

- **`src/lib/lambda-client.ts`（SigV4 署名）** — 呼び出す Lambda が存在してから（ADR 0003 / 0014）
- **エンドユーザー認証の実装**（Cognito トークンの BFF 終端・検証）— ADR 0016、認証基盤の Terraform の後
- **画面・コンポーネントの実装** — `docs/specs/*-ui.md` は揃っているが本 Issue では着手しない
- **OpenNext の導入・ビルド / デプロイ設定** — ADR 0013、Terraform の Issue
- **Claude Design / DesignSync の同期運用** — ADR 0015。本仕様が決めるのは配置先だけ（AC-1-5）
- **`src/lib/rate-limit.ts`（Route Handler のレート制限）** — 2026-08-14 の決定（U-1 / 案1）により本 Issue から外した。**スタブ・TODO・既定値の仮置きも置かない**（AC-8-5）
- **Route Handler（`src/app/api/**`）の実装** — レート制限のパラメータ（U-1 の a〜e）が未決であり、最初の Route Handler を作る Issue でセットで決める（AC-9）
- **Node.js の版の変更** — 24 に固定済み（P-2）
- **`.devcontainer/` の変更**（allowlist / Dockerfile）— `docs/specs/devcontainer-go-module-policy.md` の非スコープであり、必要になったら人間の決定が要る（P-3）

---

## 前提（P）

### P-1. ハーネスの現況（実測 2026-08-14）

| 場所 | 現況 |
|---|---|
| `Makefile` `test-web` / `lint-web` | `apps/web/package.json` が無ければ `SKIP: ...（Next.js 未スキャフォールド）` を出して素通りする |
| `Makefile` `lint-web` の実体 | `cd apps/web && npm run lint && npx tsc --noEmit` |
| `Makefile` `test-web` の実体 | `cd apps/web && npm test` |
| `.github/workflows/ci.yml` `web` ジョブ | `name: Web (lint / test)`。`apps/web/package.json` があれば `npm ci`、無ければ SKIP を echo する分岐を持つ |
| `docs/harness/verification-loop.md` | 「未スキャフォールドのレイヤーは『SKIP』を明示する」節が `apps/web` と `infra/terraform` を挙げ、`test-web` の SKIP 出力を例示している |
| `README.md` | 「Web（`apps/web`）| 未スキャフォールド」（現況表）、「Next.js / TypeScript | 未定」（版の表）、SKIP の例示に `test-web` を使用 |

**`lint-web` / `test-web` が呼ぶコマンドは Makefile 側が既に確定している。** 本仕様はこの呼び出しを変えず、`apps/web` 側をそれに合わせる（AC-2）。

### P-2. Node.js の版は 24 で固定済み

`.devcontainer/Dockerfile`（`FROM node:24`）と `.github/workflows/ci.yml`（`node-version: 24`）の2箇所で固定され、README の「バージョンと固定箇所」表に載っている。**片方だけ上げると「ローカルで通るが CI で落ちる」が起きる**（`docs/rules/commands.md`）。本仕様は版を変えない。

### P-3. devcontainer 内で npm の依存取得が成立するかは未実測

`.devcontainer/init-firewall.sh` は `registry.npmjs.org` を allowlist に持つが、**追加されるのは起動時に `dig` が返した A レコードだけ**である。この方式は Go モジュールで実際に破綻しており（`docs/specs/devcontainer-go-module-policy.md` P-2）、同仕様 AC-10-6 は「npm 等は対象外。スキャフォールド時に同種の問題が再発しうる」と明記している。

- **npm の取得が devcontainer 内で成立するかは、本仕様の時点で未実測である**（Go と違い `registry.npmjs.org` は複数 A レコードを返しうるため、同じ結論になるとは限らない）
- **成立しなかった場合、allowlist や `.devcontainer/Dockerfile` を変更して解決してはならない。** それは `devcontainer-go-module-policy.md` の非スコープであり、**人間の決定**が要る（同 AC-2）。実装工程は停止して人間へ上げる

### P-4. `.gitignore` は Node/Next.js 節を既に持つ

`node_modules/` `.next/` `out/` `build/` `dist/` `*.tsbuildinfo` `next-env.d.ts` `coverage/` などが登録済み。**`package-lock.json` は無視されていない**（コミットできる）。**`next-env.d.ts` は無視されている**ため、clone 直後の作業ツリーには存在しない（AC-6-2 の前提）。

### P-5. ADR 0007 の Go 向け制約は Web に自動適用されない

ADR 0007 の §1〜§4（testify を採らない / go-cmp のみ許す / モックライブラリを入れない / `check-domain-deps` の強化）は **Go のテストと `services/api/internal/domain` を対象**にした決定である。Web に及ぶのは §5「`apps/web` のテストは Vitest とする。導入はスキャフォールド（#9）の時点で行う。本 ADR は選定のみを決め、設定は #9 の範囲とする」のみ。

**`make check-domain-deps` は Web を検査しない。** Web 側の依存規律に相当する機械検査は存在せず、本仕様でも作らない（AC-10-2 に限界として固定する）。

### P-6. デプロイ形態は OpenNext（ADR 0013）

本 Issue はアダプタを導入しないが、**OpenNext と非互換になる構成を採らない**。具体的には静的エクスポート（`output: 'export'`）を採らない。Route Handler / SSR がサーバー実行を要することは ADR 0013 が「素の S3 + CloudFront」を却下した理由そのものである。

---

## 受け入れ条件（AC）

| AC | 主題 | 主に効く工程 |
|---|---|---|
| AC-1 | スキャフォールドの構成と配置 | implementer・reviewer |
| AC-2 | `package.json` の scripts 契約（Makefile が呼ぶ） | tester・implementer |
| AC-3 | テスト基盤は Vitest。0件で緑にしない | tester・implementer |
| AC-4 | Makefile — SKIP を外し、対象が無ければ失敗させる | tester・implementer |
| AC-5 | CI の `web` ジョブが実際に検査していること | implementer・reviewer |
| AC-6 | clone 直後から決定的に走ること | tester・implementer |
| AC-7 | docs / README の同期（実装コミットと同時） | implementer・reviewer |
| AC-8 | セキュリティ・コスト・依存の最小化 | implementer・reviewer |
| AC-9 | 本 Issue で実装しないものの固定 | reviewer |
| AC-10 | 限界 — 緑が意味しないこと | reviewer |
| AC-11 | **決定済み（2026-08-17）**: gitleaks の allowlist を `apps/web/.next/` に限定して導入する | tester・implementer・reviewer |
| AC-12 | `.gitleaks.toml` の**構造をホワイトリスト型で機械検査する**（AC-11 の検査が素通りさせる弱体化を捕捉する） | tester・implementer・reviewer |
| U-1 | **決定済み（2026-08-14 / 案1）**: レート制限は本 Issue から外す。パラメータ a〜e は次 Issue の入力として残す | 次 Issue の specifier |

### AC-1. スキャフォールドの構成と配置

| # | 要求 |
|---|---|
| 1-1 | アプリの実体を **`apps/web`** に置く（Makefile の `WEB_DIR` と一致させる）。**リポジトリ直下に `package.json` / npm workspaces を作らない。** Makefile は `cd apps/web && npm ...` を呼ぶ（P-1）ため、ルートに寄せるとハーネスの前提が変わる |
| 1-2 | **App Router**（`apps/web/src/app/`）を使う。Pages Router（`pages/`）を併設しない。ADR 0013 / 0016 / 各 UI 仕様が App Router と Route Handler を前提にしている |
| 1-3 | **TypeScript**。`tsconfig.json` の `strict` を `true` にする。`npx tsc --noEmit`（`lint-web` が呼ぶ）で型エラーが赤くなること |
| 1-4 | **Tailwind CSS** を導入し、少なくとも1つのページ（`/`）で実際に適用されていること。ページの中身は最小でよい（画面仕様の実装を始めない。AC-9） |
| 1-5 | **ローカル成果物（コンポーネント）の配置先は `apps/web/src/components/`。2026-08-14 に人間が承認した決定であり、AI の仮置きではない。** ADR 0015 が「`apps/web` スキャフォールド時に別途確定する」として委譲した先が本仕様のこの項であり、**これをもって当該委譲は解消する**（以後 ADR 0015 の「未確定」は本項を指す）。リポジトリ直下の `ui_kits/` は**採らない**（Next の同一デプロイに収める。ADR 0013。`docs/rules/architecture.md` が名指しする `apps/web/src/lib/` と同じ階層規則に揃える）。**本 Issue で決めるのはディレクトリの位置だけで、コンポーネントは作らない**（AC-9） |
| 1-6 | **`apps/web/src/lib/` を用意する。** `lambda-client.ts` の置き場所として `docs/rules/architecture.md` と ADR 0003 / 0014 がパスを名指ししている。**`lambda-client.ts` 自体は本 Issue で作らない**（AC-9） |
| 1-7 | **`apps/web/.gitignore` を新設しない。** ルートの `.gitignore` が Node/Next 節を持つ（P-4）。スキャフォールドツールが生成した場合は削除する（二重管理を作らない） |
| 1-8 | スキャフォールドツールが生成する定型ファイルのうち、**内容が事実に反するものを残さない。** とくに生成 README の「Deploy on Vercel」導線は ADR 0013（Vercel から離脱）と矛盾するため削除するか、AWS ネイティブである旨に置き換える |
| 1-9 | `next.config` で **`output: 'export'`（静的エクスポート）を採らない**（P-6） |

### AC-2. `package.json` の scripts 契約

**Makefile 側のコマンドを変えずに通ること**が要件である（P-1）。

| # | 要求 |
|---|---|
| 2-1 | `apps/web/package.json` の `scripts` に **`lint`** と **`test`** が存在する。`make lint-web` は `npm run lint && npx tsc --noEmit` を、`make test-web` は `npm test` を呼ぶ |
| 2-2 | いずれも **watch せず、非対話で終了する。** 成功で終了コード 0、失敗で非 0 を返す。Vitest の既定（`vitest`）は watch モードで CI がハングするため **`vitest run`** を使う |
| 2-3 | **`package-lock.json` をコミットする。** CI の `web` ジョブは `npm ci` を使う（P-1）ため、lockfile が無いとジョブが落ちる |
| 2-4 | scripts 名を変える場合は **`Makefile` と `.github/workflows/ci.yml` を同時に直す。** 片方だけ変えない（ハーネスの前提が壊れる） |
| 2-5 | `lint` の実体（ESLint 等）は実装工程が選んでよいが、**`--fix` を既定にしない**（検査が自動修正で緑になると、何が違反だったか記録に残らない） |

### AC-3. テスト基盤は Vitest

| # | 要求 |
|---|---|
| 3-1 | テストランナーは **Vitest**（ADR 0007 §5）。Jest を入れない |
| 3-2 | **テストが0件のとき緑にしない。** `passWithNoTests` を有効にしない。0件で成功する設定は、`check-domain-deps` の「検査対象が無い場合は失敗させる」と同じ理由で偽の Green にあたる |
| 3-3 | **意味のあるテストを最低1件置く。** `expect(true).toBe(true)` のような、対象が壊れても落ちないテストを置かない（`docs/harness/verification-loop.md`「Red を踏んでいないテストは、対象が壊れても落ちない可能性を排除できていない」） |
| 3-4 | そのテストが **Red を踏めること**を tester 工程が確認する。実装（またはテスト対象の値）を壊すと落ちることを実測する |
| 3-5 | **アサーション / モックのライブラリを追加しない。** Vitest 標準の `expect` を使う。ADR 0007 の Go 向け制約は Web に自動適用されない（P-5）が、**「入れる」判断も本仕様の範囲外**であり、必要になった Issue で判断する。本 Issue はコンポーネントを作らないため `@testing-library/*` も要らない |
| 3-6 | テスト設定（`vitest.config.ts` 等）は `apps/web` 配下に置く。リポジトリ直下へ出さない |
| 3-7 | **3-4 の例外 — テストランナー自体を導入する Issue に限る。** ランナーが未導入の時点では意味のある Red を踏めないため、tester 工程による確認に代えて、**implementer 以外（reviewer）が独立にミューテーション実測を行い、その証跡を本仕様に記録する**。**「tester が確認できない」は「誰も確認しなくてよい」を意味しない。** 適用条件と本 Issue での実測結果は「AC-3-7」節（下記）に記す |

#### AC-3-7. Red の確認の主体 — 2026-08-17 の決定（案(a)）

**AC-3-4 の本文は変更しない。** 通常の Issue では引き続き「そのテストが Red を踏めることを **tester 工程が**確認する」が有効である。本項が定めるのは、テストランナー自体を導入する Issue に限った例外と、その場合に Red を担保する代替手段である。

| 項目 | 内容 |
|---|---|
| 決定日 | 2026-08-17 |
| 決定者 | 人間（AI の仮置きではない） |
| 採った案 | **案(a)** — reviewer が実測した Red 証跡を記録して閉じる。**コード・履歴には触れない。force-push しない** |
| 採らなかった案 | 案(b) — 履歴を `test:` / `feat:` へ分割し直す（force-push を要する） |
| 記録先 | **本仕様**（PR コメント・Issue コメントで終わらせない。ADR 0004） |

##### 何が起きたか（逸脱の事実。無かったことにしない）

| 項目 | 実測 |
|---|---|
| 対象 | 本 Issue の唯一の Vitest テスト `apps/web/src/next.config.test.ts` |
| 実際の混入先 | **`feat:` コミット `15dc5e6`**（`test:` コミットではない） |
| `test:` コミット `944e231` の中身 | シェル fixture 2本のみ（`.github/scripts/test-makefile-web.sh` / `.github/scripts/test-apps-web-contract.sh`） |
| 帰結 | **AC-3-4 が要求する「tester 工程が Red を確認する」が満たされていない。** `docs/rules/commit-convention.md`「仕様 → テスト → 実装の順序が履歴に残る」に対する逸脱でもある |

##### なぜ構造的なのか（怠慢ではない）

tester 工程の時点では **Vitest 自体が未導入**であり、テストランナーが存在しない。ランナー不在の状態で得られる失敗は「ランナーが無い」型のエラーであって、**「対象が壊れているから落ちた」ことを示さない**（AC-3-3 が排除しようとしている偽の Red と同型）。意味のある Red を踏めるのは、ランナーとテスト対象（`apps/web/next.config.ts`）が揃った実装工程以降である。したがってこの逸脱は、**テストランナー自体を導入する Issue に固有の構造**による。

##### 何をもって Red を担保したか（reviewer が独立に実測。計2回）

| 往復 | 実測者 | 操作 | 結果 |
|---|---|---|---|
| 往復1 | **reviewer**（implementer ではない） | `apps/web/next.config.ts` に **`output: "export"` を注入**するミューテーション（AC-1-9 / P-6 が禁じる構成へ壊す） | `next.config.test.ts` が **AssertionError で FAIL（rc=1）**。実測後に復元し、ワークツリーがクリーンであることを確認 |
| 往復2 | **reviewer** | 同じミューテーションを**再実測（2回目）**。加えて、テストを `src/app/` から `src/` へ移動した（W-3 対応）後も検出力が落ちていないことを、テストを退避して2系統で実測 | ミューテーションは同じく FAIL。退避時は**契約テスト exit 1** / **`make test-web` exit 2** の2系統で Red |

再現手順は上表のとおり（変異内容・失敗の型・終了コードを含む）。**「ランナーが無い」型の失敗ではなく AssertionError であること**が、この実測が Red の担保になる根拠である。

##### 例外の適用条件（これを満たさない Issue へ広げない）

| # | 条件 |
|---|---|
| a | その Issue が**テストランナー自体の導入**を含み、tester 工程の時点でランナーが存在しないこと |
| b | 代替の Red 実測を **implementer 以外**（reviewer）が独立に行うこと |
| c | 実測が**ミューテーション**（テスト対象を壊す）であること。**失敗の型（AssertionError か否か）と終了コードを記録**し、「ランナーが無い」型の失敗と区別すること |
| d | 実測後に**変異を復元**し、ワークツリーがクリーンであることを確認すること |
| e | 実測の**主体・回数・変異内容・結果を本仕様（`docs/`）に記録**すること |
| f | **ランナー導入後の Issue には適用しない。** 以後は AC-3-4 のとおり tester 工程が Red を確認する |

### AC-4. Makefile — SKIP を外し、対象が無ければ失敗させる

| # | 要求 |
|---|---|
| 4-1 | `test-web` / `lint-web` から **`apps/web/package.json` が無ければ SKIP する分岐を削除する。** SKIP 分岐を残すと、`apps/web` を消す・壊す変更が緑のまま通る |
| 4-2 | **対象（`apps/web/package.json`）が無い場合は成功ではなく失敗を返す。2026-08-14 に人間が承認した決定。** `check-domain-deps` の「検査対象が無い場合は失敗させる」と同じ扱い（`docs/harness/verification-loop.md`）。スキャフォールド済みである以上、`package.json` の不在は「未着手」ではなく「壊れている」を意味する |
| 4-3 | 失敗時は**黙って落ちない**。何が無いのか（`apps/web/package.json`）と、それが失敗である理由をログに出す |
| 4-4 | **`make lint-web` / `make test-web` の出力に `SKIP` の語が現れないこと。** これが Issue の完了条件「SKIP ではなく実際に走る」の機械的な確認手段である |
| 4-5 | **`lint-tf`（`infra/terraform`）の SKIP は据え置く（2026-08-14 の決定に含む）。** infra は未着手であり、本 Issue の対象ではない。**web の扱い（4-2）を infra へ波及させない** |
| 4-6 | **web 向けの新しい make ターゲットを増やさない。** `verify` / `test` / `lint` の依存関係を変えない（`docs/rules/commands.md` の表を増やさない＝毎回読まれるトークンを増やさない） |

### AC-5. CI の `web` ジョブが実際に検査していること

| # | 要求 |
|---|---|
| 5-1 | `.github/workflows/ci.yml` の `web` ジョブの依存インストール step から **SKIP の else 分岐を削除**し、`cd apps/web && npm ci` を無条件に実行する。`package.json` が無ければジョブが落ちる（AC-4-2 と同じ理由） |
| 5-2 | **ジョブの `name:`（`Web (lint / test)`）を変えない。新規ジョブを起こさない。** `name:` の変更は ruleset `protect-main` の必須チェックを静かに外す（`docs/harness/verification-loop.md`「必須チェックへの登録」）。人間のブラウザ操作を発生させない |
| 5-3 | **`node-version: 24` を変えない**（P-2）。変えるなら `.devcontainer/Dockerfile` と同時 |
| 5-4 | `actions/setup-node` の npm キャッシュを足すかは任意。足す場合は `cache-dependency-path` を `apps/web/package-lock.json` に指定し、**キャッシュの有無で結果が変わらない**こと |
| 5-5 | **CI 側にだけ存在する検査を作らない。** `web` ジョブが呼ぶのは `make lint-web` / `make test-web` のみに保つ |

### AC-6. clone 直後から決定的に走ること

| # | 要求 |
|---|---|
| 6-1 | **生成物が一切無い作業ツリー**（`git clone` 直後相当）で `npm ci` を行っただけの状態から、`make lint-web` と `make test-web` が成功すること。**`next build` / `next dev` を先に走らせる必要があってはならない**（CI はそれらを実行しない） |
| 6-2 | とくに **`next-env.d.ts` は `.gitignore` 済みで clone 直後に存在しない**（P-4）。`npx tsc --noEmit` がその不在で落ちない構成にすること。手段は実装工程が選んでよい（例: `tsconfig.json` の `types` / `include` の調整、生成物由来の型に依存しない）。**既定では `.gitignore` から `next-env.d.ts` を外して生成物をコミットする案を採らない。** 採るなら本仕様を更新して理由を残す |
| 6-3 | `npm ci` 以降、**lint / test がネットワークアクセスを要求しない**こと。テストから外部へ通信しない（devcontainer の allowlist に依存する検査を作らない） |
| 6-4 | **決定的であること。** 現在時刻・タイムゾーン・ロケール・乱数に依存して結果が変わるテストを置かない（`docs/harness/verification-loop.md` のハーネスの性質1） |
| 6-5 | `make verify` が通ること（`lint` + `test` + `check-domain-deps` + `check-skills` + `check-go-module-pins` + `scan-secrets`） |

### AC-7. docs / README の同期 — 実装コミットと同時に行う

**ターゲットや構成が変わっていない時点で docs を書き換えると docs が虚偽になる**（`docs/specs/devcontainer-go-module-policy.md` AC-9-4 と同じ扱い）。

| # | 要求 |
|---|---|
| 7-1 | `docs/harness/verification-loop.md`「未スキャフォールドのレイヤーは『SKIP』を明示する」節から **`apps/web` を外す**（`infra/terraform` は残す）。例示している SKIP 出力（`==> test-web`）を、実際に SKIP が残る側（`lint-tf`）の例へ差し替える |
| 7-2 | 同 doc に、**web は対象が無ければ失敗する**（AC-4-2）ことを書く。「対象が無い場合に失敗させるのはドメイン層の依存検査だけ」という現行の記述が、実態と食い違ったまま残らないようにする |
| 7-3 | README の次の3点を実態へ合わせる: (a) 現況表「Web（`apps/web`）| 未スキャフォールド（ハーネスは `SKIP` を明示）」、(b) 「バージョンと固定箇所」表の「Next.js / TypeScript | 未定」を実際に入った版と固定箇所（`apps/web/package.json` / `package-lock.json`）へ、(c) SKIP を例示している箇所（`test-web` を使っている） |
| 7-4 | **同期を口実に未完了を隠さない**（`docs/specs/readme-adr-sync.md` AC-3-4）。画面・BFF（`lambda-client.ts`）・Cognito 認証・デモURL・インフラは未完了のままであり、その表現を残す。**「Web が動く」と読める記述にしない** |
| 7-5 | **`docs/rules/` を変更しない。** 本 Issue はコマンドもガードレールも増やさない（毎回読まれるトークンを増やさない。ADR 0010 §B） |
| 7-6 | **本仕様が構成の決定の正解の置き場所である。** 決定の理由を Issue コメント・PR 本文・コミットメッセージへ書き写さない（ADR 0004）。PR 本文には本仕様へのリンクを置く（CI の `spec-link`） |

### AC-8. セキュリティ・コスト・依存の最小化

| # | 要求 |
|---|---|
| 8-1 | **`.env` 系ファイルを追加しない。** 必要になったら `.env.example`（中身は必ずダミー値）のみをコミットし、**コミット前に** `.gitignore` を確認する（`docs/rules/security.md`） |
| 8-2 | 生成・作成するファイルに、実在の URL・AWS アカウント ID・接続文字列・鍵を含めない |
| 8-3 | **`make scan-secrets` が `apps/web/node_modules` の存在する作業ツリーで通ること。** 通らない、または実用的な時間で終わらない場合、**gitleaks の除外設定を黙って足さずに停止して人間へ上げる**。検査の弱体化と改善は AI 自身から区別できない（ADR 0010 §A） |
| 8-4 | **依存を最小限にする。** AC-1〜AC-6 を満たすのに不要なパッケージを入れない。UI ライブラリ・状態管理・HTTP クライアント・認証ライブラリを先回りして入れない |
| 8-5 | **`src/lib/rate-limit.ts` を本 Issue で作らない**（U-1 を 2026-08-14 に案1で決定＝本 Issue から外した）。**素通しの実装・TODO だけの空実装・既定値の仮置きを置かない。** 「あるように見えて効いていないガードレール」は、無いことより悪い。ファイルが存在しないことが正である |

### AC-9. 本 Issue で実装しないもの（対象外の固定）

**以下が無いことは欠落ではなく仕様である。** レビューで「足りない」と指摘しない。

| 対象 | いつ / どこで | 根拠 |
|---|---|---|
| `src/lib/lambda-client.ts`（実行ロールでの SigV4 署名） | 呼び出すドメイン API Lambda が存在してから（Terraform の後） | ADR 0003 / 0014、`docs/rules/architecture.md` |
| Cognito トークンの BFF 終端・検証、ログイン3方式 | 認証基盤の Terraform の後 | ADR 0016、`docs/specs/login-ui.md` |
| 画面・コンポーネントの実装（`src/components/` の中身） | 後続の画面 Issue。本 Issue は置き場所のみ確定（AC-1-5） | `docs/specs/work-month-screen-ui.md` ほか、ADR 0015 |
| **`src/lib/rate-limit.ts`（レート制限）** | **最初の Route Handler を作る Issue。** U-1 を 2026-08-14 に**案1**で決定し、本 Issue から外した。**スタブも置かない**（AC-8-5） | U-1、`docs/rules/cost-guardrails.md`、ADR 0010 §A |
| Route Handler（`src/app/api/**`） | **U-1 が決まってから。** 最初の Route Handler を作る Issue は、レート制限の決定（U-1 の a〜e をセットで）なしに着手できない。**この着手条件は案1の決定後も外れない** | `docs/rules/cost-guardrails.md`、CLAUDE.md（ガードレールを「後で入れる」ことを許容しない） |
| OpenNext の導入・ビルド / デプロイ設定 | Terraform の Issue | ADR 0013 |
| Claude Design / DesignSync の同期運用 | 画面 Issue | ADR 0015（本仕様は配置先のみ確定。AC-1-5） |
| ドメイン API の HTTP 呼び出し実装 | `lambda-client.ts` と同時 | `docs/specs/domain-api-http-contract.md` |

### AC-10. 限界 — 緑が意味しないこと

**以下は仕様どおりの限界であり、欠陥ではない。**

| # | 内容 |
|---|---|
| 10-1 | **「SKIP が出ない」は「意味のある検査をした」を意味しない。** `passWithNoTests` の禁止（AC-3-2）で0件は防げるが、テストの中身が薄いことは防げない。緑はテストが主張した範囲の意味しか持たない |
| 10-2 | **Web には `check-domain-deps` に相当する依存検査が無い。** 「アサーション / モックライブラリを入れない」（AC-3-5）は機械検査されず、規律で守る。ADR 0007 の許可リストは Go の `domain` にしか効かない（P-5） |
| 10-3 | **`npm ci` の再現性は lockfile に依存する。** lockfile を更新した PR で依存が何に入れ替わったかを、この検査は意味的に評価しない |
| 10-4 | **CI と devcontainer で非対称がある。** GitHub runner に egress 制限は無く、`npm ci` は必ず通る。**CI が緑であることは、devcontainer で `npm ci` できることを意味しない**（P-3。`docs/specs/devcontainer-go-module-policy.md` AC-10-9 と同型） |
| 10-5 | **本 Issue の完了は「ハーネスが web を実際に検査している」ことであり、アプリが動く・デプロイできることを意味しない。** OpenNext も Terraform も未着手である |
| 10-6 | **コストガードレールの1枚（Route Handler のレート制限）は本 Issue 完了時点で未実装のままである。これは 2026-08-14 の決定（U-1 / 案1）による意図した状態であり、欠落ではない。** 本 Issue にレート制限の対象（Route Handler）が存在しないことと整合する。「後で入れる」で流さないための担保は、対象を作る Issue の着手条件として U-1 を AC-9 に固定したこと**のみ**であり、**機械検査は無い**（規律で守る） |
| 10-7 | **Tailwind が「適用されている」ことは機械検査しない**（AC-1-4）。ビルドと型検査は通るが、見た目の妥当性はハーネスの外にある |

### AC-11. gitleaks の allowlist — `apps/web/.next/` に限定して導入する

**2026-08-17 に人間が確定した決定。** 要否は tester / implementer / reviewer の判断対象ではない。AC-8-3 が定めた停止条件（「gitleaks の除外設定を黙って足さずに停止して人間へ上げる」）が実際に作動し、その結果として人間が決めたものである。**AC-8-3 は緩めない**（`apps/web/.next/` 以外へ広げる場合の停止条件は生きている。AC-11-9）。

> U-1 の「案2」に現れる `AC-11` は**採らなかった案の中の仮の採番**であり、本節とは無関係である（レート制限のパラメータは本 AC に含まれない）。

#### 訂正の記録（2026-08-18） — 本 AC には事実に反する記述があった

**無かったことにしない。** 実装後のレビューで、本 AC の3箇所が実測に反していることが判明した。**決定（`apps/web/.next/` に限定して除外する）自体は変わらない。** 変わったのは「その決定が機械でどこまで守られているか」の記述である。

| 箇所 | 誤っていた記述 | 実測に基づく訂正 |
|---|---|---|
| 11-9-c | 「機械的な歯止めは 11-4 のテストベクタが担う（範囲を広げる変更はテストを落とす）」 | **誤り。** 3種類の弱体化が現行の検査を **12/12 通過で素通り**する（下表）。9-c を実測どおりに書き換え、捕捉は **AC-12** に新設した |
| 11-L1 | 盲点を「作業ツリー走査（`gitleaks dir`）の対象外になる」とだけ書いていた | **過少。** `.gitleaks.toml` は **`gitleaks git`（履歴側）にも適用される**。履歴側でも見えなくなる |
| 11-7-b | 「`gitleaks git` にも適用されるかは未確認」（11-7-c が実測値への置換を課していたが未実施） | 実測値へ置き換えた（11-7-b / 7-d） |

##### 訂正の根拠となった実測（2026-08-18 / gitleaks 8.30.1。3者が独立に再現）

| 弱体化 | 現行検査の結果 | 実害 |
|---|---|---|
| `paths` の正規表現から **`^` アンカーだけ**を削る（`apps/web/\.next/` の文字列は残す） | **12/12 通過** | `vendor/apps/web/.next/` のような**位置ずれしたパス**が除外対象に入る |
| **2本目の `[[allowlists]]`** を増設する | **12/12 通過** | 11-3-a（要素は1個）・11-3-e（定義は1箇所）が機械的に守られていない |
| **既存の `[extend]` テーブル内**に `disabledRules = ["generic-api-key"]` を1行足す | **12/12 通過** | **リポジトリ全域で検出が沈黙する。** `.next` の外に検出対象を置いた対照実験で `leaks found: 2` → `no leaks found` |

3つとも **`[extend] useDefault = true` を残したまま**成立する。すなわち 11-2 が防ごうとした偽 Green が、`useDefault` とは**別の経路**で復活していた。

#### 事実（実測 2026-08-17。推測ではない）

| 項目 | 実測値 |
|---|---|
| 現象 | `apps/web/.next` が存在する作業ツリーで `make scan-secrets` が **EXIT=1**（`leaks found: 3`） |
| 検出内容 | `generic-api-key` × 3。`apps/web/.next/prerender-manifest.json`（126・127行目）、`apps/web/.next/server/server-reference-manifest.json`（4行目） |
| 正体 | Next.js が**ビルドのたびに生成するランダムなプレビュー用の鍵**。ソースコード由来ではなく、人が書いた秘密でもない |
| コミット経路 | `apps/web/.next` はリポジトリ直下の `.gitignore` で除外済み。**コミットされる経路が存在しない** |
| 履歴側 | `gitleaks git .` は**現状も通る**（`.next` が履歴に入ったことがない） |
| 現在の設定 | **`.gitleaks.toml` は存在しない。** gitleaks 8.30.1 の組み込みルールだけで動いている |
| `make scan-secrets` の実体 | `gitleaks dir . --no-banner --redact` と `gitleaks git . --no-banner --redact` の2本 |

**除外してよい根拠は上の4点（生成物である・値がビルドごとのランダムである・コミット経路が無い・履歴側は無変更）であって、「うるさいから」ではない。** この4点のいずれかが崩れたら除外の根拠も崩れる（AC-11-L2）。

#### 要求

| # | 要求 |
|---|---|
| 11-1 | **リポジトリ直下に `.gitleaks.toml` を新設する。** `make scan-secrets` の2本はいずれも source を `.`（リポジトリ直下）として起動するため、この位置に置く。**Makefile へ `--config` を足さずに読まれること**を実測で確かめる（読まれていなければ 11-6 の実測結果が現状から変化しない） |
| 11-2 | **`[extend]` の `useDefault = true` を必ず入れる。** `.gitleaks.toml` を新設すると、既定では gitleaks の組み込みルールが**無効化**される。ここを落とすと「設定を足したら全ルールが消え、何も検出しなくなり、しかも緑になる」という**偽 Green** になる。本件で最も危険な失敗モードであり、11-6 で機械検査する |
| 11-3 | **除外は `apps/web/.next/` 配下のパスに限定する。** 表現はグローバル allowlist の `paths`（正規表現の配列）**のみ**を使い、次を満たすこと。<br>(a) `paths` の要素は **1個**（複数書かない）<br>(b) 正規表現は**先頭がアンカーされている**。既定形は `^apps/web/\.next/`<br>(c) `.` を `\.` でエスケープする（`.next` の `.` を任意1文字にしない）<br>(d) 末尾は `/` で終わる（配下に限定し、兄弟ディレクトリを含めない）<br>(e) 定義を1箇所に置く（ルール単位の allowlist や複数の allowlist 定義へ分散させない）<br>(f) gitleaks が評価するパス表現が相対か絶対かは**実装工程で実測**する。絶対パスで評価される場合に限り `(^|/)apps/web/\.next/` を採ってよい。**それ以外の緩め方をしない**<br>**（2026-08-18 追記。(f) の本文は変更していない）** 完全一致（12-2-d-1）の導入により、(f) の発動には**許可値の差し替え = 人間の承認**（12-7-a）が要る。**実装工程が実測を根拠に独断で採れる選択肢ではなくなった**（12-4-e-2） |
| 11-4 | **11-3 の正規表現は次のテストベクタを満たすこと**（この表がテストの入出力になる） |
| 11-5 | **使ってはならない表現**（11-4 で落ちるはずだが、意図としても禁止する） |
| 11-6 | **`useDefault = true` が効いていることを機械検査する** |
| 11-7 | **`gitleaks git .`（履歴側）の呼び出しは無変更。** ただし**設定の適用範囲は履歴側にも及ぶ**（無変更なのはコマンドラインであって走査範囲ではない。11-7-d） |
| 11-8 | **`make scan-secrets` が EXIT=0 で終わること。** `apps/web/.next` が存在する作業ツリー（`npm run build` 後）と、存在しない作業ツリー（clone 直後相当）の**両方**で成功すること。後者で `.gitleaks.toml` が原因の失敗（設定の構文エラー等）が出ないこと |
| 11-9 | **allowlist を広げる変更には人間の承認が要る**（歯止め） |
| 11-10 | **gitleaks のバージョン依存を明記する** |

##### 11-4. パスのテストベクタ

| 入力パス | 期待 |
|---|---|
| `apps/web/.next/prerender-manifest.json` | **一致**（除外される） |
| `apps/web/.next/server/server-reference-manifest.json` | **一致**（除外される） |
| `apps/web/.next/` 配下の任意の深さのファイル | **一致**（除外される） |
| `apps/web/src/lib/lambda-client.ts` | **不一致**（除外されない） |
| `apps/web/next.config.ts` | **不一致** |
| `apps/web/.next.bak/prerender-manifest.json` | **不一致**（兄弟ディレクトリを巻き込まない） |
| `apps/web/xnext/a.json` | **不一致**（`.` をエスケープしていれば落ちない） |
| `apps/web/node_modules/pkg/.next/a.json` | **不一致**（`node_modules` は本 AC の対象外） |
| `docs/.next/a.json` | **不一致**（任意位置の `.next` を許さない） |
| `services/api/.next/a.json` | **不一致** |
| `infra/terraform/main.tf` | **不一致** |

**（2026-08-18 追記。上表は変更していない。12-4-b の「11-4 の表は変更しない」に従う。）** 完全一致（12-2-d-1）の導入後も**この表と、その11本を評価する検査は残す**。位置づけを取り違えないこと。

| 観点 | 内容 |
|---|---|
| この表が担うもの | **除外範囲の「意味」の定義。** `^apps/web/\.next/` という文字列が**なぜその文字列でなければならないか**を示す唯一の記述であり、許可値を人間が差し替えるとき（12-2-d-1-e）に新しい値の妥当性を判定する根拠になる |
| この表が担わ**ない**もの | **弱体化を捕捉する機械的な歯止め。** 11-9-c のとおり、11本の判定を変えない弱体化（`^` の削除・末尾の緩和・選択肢の追加）は素通りする。歯止めは **12-2-d-1 の完全一致**が担う |
| なぜ冗長でも残すか | 完全一致は「値が1つに固定されていること」しか見ず、**その1つが正しい範囲かを見ない**。表を消すと、許可値の差し替え時に判断の根拠が消える。あわせて、完全一致の検査コードが壊れた場合の**多層防御**として独立に赤くなる（12-4-f と同じ理由） |

##### 11-5. 使ってはならない表現

| 表現 | なぜ駄目か |
|---|---|
| アンカーの無い `apps/web/\.next/` / `\.next/` | 任意位置の `.next` を許し、リポジトリのどこにでも「見えない場所」を作れる |
| `.*next.*` / `next` などの部分一致 | `next.config.ts` や `src/` 配下まで巻き込む |
| ビルド成果物一般（`dist` / `build` / `out` / `coverage` / `node_modules`） | 決定は `.next` に限定されている。落ちた実測が無いものを先回りで除外しない |
| リポジトリ全体（`.` / `^` / `.*`） | 検査を無効化するのと同じ |
| 拡張子・ファイル名単位（`\.json$` / `prerender-manifest\.json`） | 場所を限定しないため、`.next` の外の同名ファイルまで見逃す |
| 内容ベースの除外（`regexes` / `stopwords` / 特定コミットの除外） | 場所を限定しない。同じ形の文字列がソースに入っても検出されなくなる |
| ルールの無効化（`generic-api-key` を止める） | パス単位ではなく検出能力そのものを削る。allowlist の趣旨から外れる |

##### 11-6. `useDefault = true` が効いていることの機械検査

| # | 要求 |
|---|---|
| 6-a | 検査は **2つの実測の対**で行う。片方だけでは「`useDefault` の欠落で全部緑」と「allowlist が正しく効いている」を区別できない。<br>(1) **`.next` の外**に置いたダミーのシークレットが**検出される**（EXIT≠0）<br>(2) **`apps/web/.next/` 配下**に置いた同じダミーが**検出されない**（EXIT=0） |
| 6-b | ダミーは gitleaks 8.30.1 の組み込みルールが検出する形式の文字列とする（例: AWS アクセスキー ID 形式 = `AKIA` + 大文字英数字16文字）。**本仕様にも、リポジトリに残るファイルにも、その文字列を実体で書かない**（`make scan-secrets` 自身が落ちる）。<br>**ただし「形式を満たす = 検出される」ではない。** 形式が合っていても検出されないダミーが実在する（実測 2026-08-17 / gitleaks 8.30.1）。ダミーの選び方は **11-6-b 補足**に従い、**検出されることを先に実測してから採用する** |
| 6-c | **検査は一時ディレクトリで完結させ、リポジトリの作業ツリーを汚さない。** ダミーを含むファイルを実行後に必ず削除し、コミットしない |
| 6-d | `.gitleaks.toml` に `useDefault = true` の記述があることを静的に確認してもよいが、**静的検査だけで済ませない**（綴り・配置が実際に効いているかは実行しないと分からない） |
| 6-e | この検査は `make verify` を汚さない形で置く。**新しい make ターゲットを増やさない**（AC-4-6 と同じ理由） |

###### 11-6-b 補足. ダミーの選び方 — 形式が合っていても検出されない値がある（実測 2026-08-17 / gitleaks 8.30.1）

**実測した事実（推測ではない）。** `AKIA` + 大文字英数字16文字という形式を満たしていても、`gitleaks dir` が検出しないダミーが存在する。**gitleaks の組み込みルール自身が `EXAMPLE` 等を含む文字列を除外している**ため（既定ルール側の allowlist であり、本 AC で新設する `.gitleaks.toml` とは別物）。

| ダミー | `gitleaks dir` の結果（8.30.1 実測） |
|---|---|
| AWS 公式ドキュメントで例示に使われる著名な値（末尾が `EXAMPLE` の形） | **検出されない**（`no leaks found` / EXIT=0） |
| ランダムな大文字英数字16文字を続けた値 | **検出される**（`leaks found: 1`） |

**これは 11-6 にとって致命的である。** 前者を選ぶと、`useDefault = true` が生きていても 6-a(1) が「検出されない」という結果になり、**「既定ルールが死んでいる」と誤診する。** 逆から見れば、`useDefault` を落とした偽 Green（11-2 が防ごうとしている最悪の失敗モード）と**区別がつかなくなる**。検査そのものが意味を失う。

| # | 要求 |
|---|---|
| b-1 | **ダミーは「検出されること自体を先に実測して」選ぶ。** allowlist を導入する**前**の状態、または `.gitleaks.toml` を外した対照で `gitleaks dir` を走らせ、そのダミーが**実際に検出される**（EXIT≠0）ことを確認してから、6-a の検査のダミーとして採用する |
| b-2 | **検出されるはずだと仮定して使わない。** 形式が合っていることを根拠に採用してはならない（上表の1行目がその反例） |
| b-3 | **著名な例示値を選ばない。** AWS 公式ドキュメント等で例示に使われる値（末尾が `EXAMPLE` のもの等）は、既定ルールが自ら除外するため使えない。ランダムな大文字英数字を用いる |
| b-4 | **実体の文字列は本仕様に書かない**（6-b と同じ理由。`make scan-secrets` 自身が落ちる）。上表もダミーの性質のみを記す |
| b-5 | 本節は **gitleaks 8.30.1 での実測**である。既定ルールの除外条件は版で変わりうるため、版を上げるときは 10-b に従って**この実測もやり直す** |

##### 11-7. 履歴側の扱い

| # | 要求 |
|---|---|
| 7-a | `gitleaks git . --no-banner --redact` の呼び出しを**変えない**。履歴側だけ別の設定を渡す（`--config` を分ける）ことをしない |
| 7-b | **実測値（2026-08-18 / gitleaks 8.30.1。7-c に従い推測を置き換えた）**: 同じ `.gitleaks.toml` は **`gitleaks git` にも適用される**。`apps/web/.next/` 配下と `src/` 配下の双方に検出対象を置いた対照実験で、`gitleaks git .` は**設定なしで `leaks found: 2`、本仕様の `.gitleaks.toml` ありで `leaks found: 1`**（`src/` のみ。`apps/web/.next/` 配下は検出されない）となった |
| 7-c | （実施済み。7-b は実測値に置き換わった）実装工程で `gitleaks git` への適用有無を確かめ、**本仕様の 7-b を実測値に置き換える**という要求だった |
| 7-d | **「履歴側は無変更」の意味を取り違えない。** 人間の決定（2026-08-17）が指す無変更は **`gitleaks git . --no-banner --redact` というコマンドラインを変えないこと**であり、**走査範囲が変わらないという意味ではない**。7-b のとおり範囲は狭まる。現状 `.next` は履歴に入ったことがないため `gitleaks git .` の EXIT は 0 のままだが、それは**除外が効いていないからではなく、除外対象が履歴に無いから**である（帰結は 11-L1） |

##### 11-9. 広げないための歯止め

| # | 要求 |
|---|---|
| 9-a | **allowlist の追加・範囲拡大・ルールの無効化は「検査の弱体化」であり、人間の承認が要る**（ADR 0010 §A の許可リストと同じ扱い）。AI が独断で行わない。**弱体化と改善は AI 自身から区別できない** |
| 9-b | `apps/web/node_modules` など**別の原因**で `make scan-secrets` が落ちた場合も、この allowlist を広げて解決しない。**AC-8-3 のとおり停止して人間へ上げる**（現時点の実測では検出3件はすべて `.next` 配下であり、`node_modules` 由来の検出は無い） |
| 9-c | **機械的な歯止めが担う範囲は限定的である**（2026-08-18 訂正。旧記述「機械的な歯止めは 11-4 のテストベクタが担う」は**事実に反していた**）。11-4 が見るのは「`paths` の正規表現が 11-4 に列挙した11本のパスをどう判定するか」だけであり、**その11本の判定を変えない弱体化は素通りする**。実測で素通りしたのは (1) `^` アンカーの削除 (2) `[[allowlists]]` の増設 (3) `[extend]` 内の `disabledRules` 追記 の3種（前掲「訂正の根拠となった実測」）。これらは **AC-12 の構造検査**で捕捉する。<br>**（2026-08-18 追記）4種目が実測された**: (4) `paths` の値に**選択肢を1本足す**（`^apps/web/\.next/\|^infra/prod`）。AC-12 の初版でも **26/26 通過**で素通りし、対照実験で `leaks found: 2` → `1` となった。原因は、AC-12 の初版が値の**型**だけを縛り、内容側の担保を再び 11-4 / 12-4 のベクタ列挙へ委譲していたこと（9-c と**同型の誤り**）。**捕捉は 12-2-d-1 の完全一致**が担う |
| 9-d | **AC-12 を足しても「広げられないこと」は保証されない。** 検査対象は `.gitleaks.toml` の**現在の記述**であり、**テスト・検査ごと書き換える変更は機械には止められない**（AC-11-L5 / AC-12-L1）。9-a の規律が最終防衛線である点は変わらない |

##### 11-10. バージョン依存

| # | 要求 |
|---|---|
| 10-a | 本 AC は **gitleaks 8.30.1** の設定スキーマ（`[extend]` の `useDefault`、グローバル allowlist の `paths`、サブコマンド `dir` / `git`）に依存する。`.gitleaks.toml` にも対象版を記す |
| 10-b | 版を上げるときは `.devcontainer/Dockerfile` と `.github/workflows/ci.yml` を同時に更新し（`docs/rules/commands.md`）、**11-6 の実測をやり直す。** 設定スキーマと既定ルールは版で変わりうる |
| 10-c | **`docs/rules/` は変更しない**（AC-7-5）。`.gitleaks.toml` に置いてよいのは「なぜ除外するか」1〜2行と**本仕様へのリンク**のみ。理由の本文を書き写さない（ADR 0004）。`docs/harness/verification-loop.md` への追記は必須としない |

#### AC-11 の限界 — この除外が意味しないこと

**AC-10 への追加であり、AC-10 の既存項は変更しない。**

| # | 内容 |
|---|---|
| 11-L1 | **`apps/web/.next/` 配下は、`gitleaks dir`（作業ツリー）だけでなく `gitleaks git`（履歴）でも対象外になる**（2026-08-18 訂正。旧記述は作業ツリーのみと書いており、盲点を過少に見せていた。実測は 11-7-b）。そこへ本物の秘密が置かれても、**`make scan-secrets` の2本のどちらでも検出されない**。**「安全になった」のではなく「この範囲で見なくなった」** |
| 11-L1-b | 帰結として、**万一 `apps/web/.next/` 配下が過去に履歴へ入っていた場合、それも検出されなくなる。** 現状は入っていない（前掲「事実（実測 2026-08-17）」の「履歴側」）ため実害は生じていないが、**限界としては事実**である。「履歴側は無変更」はコマンドラインの話であって、走査範囲の話ではない（11-7-d） |
| 11-L2 | 許容根拠は **`.gitignore` によりコミット経路が無いこと**の一点である。`.gitignore` から `.next` の除外が外れた瞬間、この根拠は崩れる。**その連動は機械検査しない**（規律で守る） |
| 11-L3 | 除外は**パス単位**であり、`generic-api-key` の3件だけを止めるのではなく、`.next` 配下の**全ルール**の検出を止める |
| 11-L4 | `make scan-secrets` の緑は「リポジトリに秘密が無い」を意味しない。gitleaks はパターン検出であり、未知の形式・伏せられた値は捕まえない（本 AC の前からある限界。本 AC で変わったのは**走査範囲だけ**） |
| 11-L5 | 11-4 / 11-6 の検査は「**現在の設定が範囲どおりか**」を見るだけで、「**将来広げられないこと**」は保証しない。テストごと書き換える変更は機械では止まらない（9-a の規律が最終防衛線） |

---

### AC-12. `.gitleaks.toml` の構造をホワイトリスト型で機械検査する

**AC-11 の訂正（11-9-c）から派生した追加要求。** 11-4 / 11-6 の検査は素通りするが検出能力を削る弱体化が実在する（前掲「訂正の根拠となった実測」）。本 AC はそれを機械で捕捉する。**AC-11 の決定・範囲を変えるものではなく、AC-11-6 の動的検査を置き換えるものでもない**（併存させる）。**AC-8-3 の停止条件は緩めない。**

#### 訂正の記録（2026-08-18） — 本 AC の初版にも同型の抜けがあった

**無かったことにしない。** 初版の 12-2-d は `paths` の値の**型**（要素1個のリテラル文字列）だけを縛り、**内容**を縛っていなかった。そのため内容側の担保が 11-4 / 12-4 のベクタ**列挙**に戻っており、**11-9-c を訂正した誤り（機械的な歯止めをベクタの列挙へ委譲する）が 12-2-d で再発**していた。

| 項目 | 実測（2026-08-18 / gitleaks 8.30.1） |
|---|---|
| 素通りした弱体化 | 本番 `.gitleaks.toml` の `paths` に選択肢を1本足す（`^apps/web/\.next/` → `^apps/web/\.next/\|^infra/prod`） |
| 初版の検査の結果 | **26/26 通過**（EXIT=0 / FAIL=0）。`\|^\.github/` / `\|\.env` も同じく素通り |
| 実害 | 同一のダミーを `infra/prod/keys.txt` と `apps/web/src/lib/x.ts` に置いた対照実験で、本番設定 `leaks found: 2` → 変異後 **`leaks found: 1`**。**`.next` と無関係なパスが沈黙する** |
| 訂正 | **12-2-d の許可値を既定形の文字列に完全一致で固定する**（12-2-d-1）。値の型ではなく値そのものを1つに固定し、**内容側に数え上げる余地を無くす** |
| 併せて訂正した矛盾 | **W-a**: 旧 12-3-j（各行に行末コメント → 合格）が実装と矛盾していた（`paths` 行に付けると検査が FAIL する）。**`paths` 行の行末コメントを不合格にする側へ寄せた**（12-2-d-2 / 12-3-r）<br>**W-b**: 12-2-b が `[extend]` の**不在**を違反にしていなかった（上限しか見ていない）。**不在も違反にする**（12-2-b / 2-b 補足 / 12-3-o / 3-p） |

#### 12-1. 方式の判断 — 列挙型ではなくホワイトリスト型を採る

| # | 要求 |
|---|---|
| 1-a | **「許可された記述はこれだけであり、それ以外の記述が1つでもあれば不合格」というホワイトリスト型で検査する。** 危険な記述を列挙して禁じる方式（ブラックリスト型）を主にしない |
| 1-b | 理由: `.gitleaks.toml` の弱体化の経路は、gitleaks の設定スキーマが持つキーの数だけ存在する（`[[rules]]` の上書き、`[allowlist]`（単数形）、`[[rules.allowlists]]`、`regexes` / `stopwords` / `commits`、`[extend]` の `disabledRules` / `path` / `url` …）。**列挙型は「まだ列挙していない経路」を常に残す**。実際、既存の検査は3経路を素通りしていた。対して本ファイルが必要とする記述は**2テーブル・2キーだけ**であり、許可側を数え上げるほうが**短く、抜けが原理的に生じない** |
| 1-c | 12-3 に挙げる個別の弱体化は、**要求そのものではなく「ホワイトリストが実際にそれを捕捉すること」を示す必須テストベクタ**として扱う。ベクタを満たしても、それ以外の記述を許してよいことにはならない |
| 1-d | **（2026-08-18 追加）ホワイトリストは「キーの集合」だけでなく「値」にも及ぼす。** 初版は許可する**キー**を数え上げながら、`paths` の**値**については型（要素1個のリテラル文字列）しか縛らず、内容の担保をベクタの列挙へ戻していた。その結果 1-b が主張する「抜けが原理的に生じない」が値については成立せず、実測で素通りした（11-9-c(4)）。**値を1つに固定できるものは固定する**（12-2-d-1）。**禁止する値を列挙して塞がない** |

#### 12-2. 許可される記述の全体（これ以外は不合格）

`.gitleaks.toml` は次の要素**だけ**で構成されること。1つでも外れたら**不合格**とする。

| # | 許可する形 |
|---|---|
| 2-a | **コメント行**（`#` で始まる行）と**空行**。**行末コメントは `paths` 行を除いて許す**（`paths` 行の扱いは 2-d-2。2026-08-18 訂正） |
| 2-b | **テーブルは `[extend]` が「ちょうど1つ」、`[[allowlists]]` が「ちょうど1つ」、合計2つだけ。多いことも少ないことも違反とする。** すなわち **`[extend]` の不在・`[[allowlists]]` の不在をいずれも不合格**にし、**上限だけを見ない**（2026-08-18 追加。理由は 2-b 補足）。それ以外のテーブル／配列テーブルを**1つも許さない**（2本目の `[[allowlists]]`、`[allowlist]`（単数形）、`[[rules]]`、`[[rules.allowlists]]`、`[allowlists.xxx]` 等を含む） |
| 2-c | **`[extend]` 内のキーは `useDefault` が「ちょうど1つ」**、値は `true` のみ。**`useDefault` 行の不在も違反とする**（2026-08-18 追加。2-b 補足と同じ理由）。他のキー（`disabledRules` / `path` / `url` / `disabledPaths` 等）を**1つも許さない** |
| 2-d | **`[[allowlists]]` 内のキーは `paths` が「ちょうど1つ」**（不在も違反）。値は**要素1個**の配列で、要素は TOML の**リテラル文字列**（`'''...'''` または `'...'`）であり、**引用符を剥がした文字列値が 2-d-1 の既定値と完全一致すること。完全一致しない値を一切許さない。** 他のキー（`regexes` / `stopwords` / `commits` / `targetRules` / `condition` / `description` 等）を**1つも許さない**（11-3-a / 11-3-e / 11-5 の「内容ベースの除外」を構造として封じる） |
| 2-e | **トップレベルの裸のキーを許さない。** ドット付きキー（`extend.disabledRules = [...]`、`allowlists.paths = [...]` 等）による同等の記述も**テーブル内に書いたものと同じく**判定すること（記法を変えれば通る、という抜けを作らない） |
| 2-f | 上記の判定は**行単位のパターン照合でも TOML 解析でもよい**が、**ドット付きキー・行末コメント・引用符内の `#` を取り違えない**こと（12-3 のベクタで確かめる） |
| 2-g | **シークレット検査の除外設定の入口をリポジトリ直下 `.gitleaks.toml` の1ファイルに固定する。** `.gitleaksignore` など gitleaks が自動で読む別経路の除外ファイルが**リポジトリに存在しないこと**を併せて検査する（存在すれば不合格）。入口が複数あると、本 AC の検査は緑のまま除外を足せる |
| 2-h | **許可される記述の行は、行頭のインデントを持たない**（既定形どおり行頭から始まる）。インデントを付けた同等の記述は**許可外**とする（2026-08-18 追加。記法を変えれば通る、という抜けを作らない。2-e と同じ趣旨） |

##### 2-b 補足. 不在を違反にする理由（2026-08-18 追加）

**上限だけを見る検査は、`[extend]` が消えた場合を捕捉しない。** `useDefault = true` を含む `[extend]` が失われると gitleaks の組み込みルールが全滅し、**何も検出しないのに緑になる**。これは 11-2 が「本件で最も危険な失敗モード」と呼んだ形そのものであり、ホワイトリスト型（12-1-a）が「これだけであること」を要求する以上、**過不足の両方**を違反にしなければ要求を満たさない。`[[allowlists]]` の不在も同じ扱いにする（実害の向きは逆＝検査が厳しくなる側だが、**除外範囲の変更は人間の決定事項**であり、機械が黙って通してよい変更ではない。範囲を変えるときは本仕様を先に更新する。11-9-a / 12-7-a）。

##### 12-2-d-1. `paths` の許可値 — 完全一致で1つに固定する（2026-08-18 決定）

**旧 2-d は値の「型」（要素1個のリテラル文字列）だけを縛り、「内容」を縛っていなかった。** そのため内容側の担保が 11-4 / 12-4 のテストベクタの**列挙**に戻っており、12-1-b が掲げた「許可側を数え上げるほうが抜けが原理的に生じない」が値の内容については成立していなかった。実測（2026-08-18 / gitleaks 8.30.1）では、本番設定の `paths` に選択肢を1本足すだけ（`^apps/web/\.next/|^infra/prod`）で、検査は **26/26 通過**する一方、対照実験では `leaks found: 2` → **`leaks found: 1`** となり、`.next` と無関係なパスが沈黙した。これは 11-9-c を訂正した「機械的な歯止めをベクタの列挙へ委譲する」誤りと**同型の再発**である。

**したがって許可値を、既定形の文字列そのものに完全一致で固定する。** 内容側に数え上げる余地が無くなり、この再発の系統が終わる。

| # | 要求 |
|---|---|
| d-1-a | **許可する文字列はこの1つだけである**: `^apps/web/\.next/` |
| d-1-b | **比較の対象**は、`[[allowlists]]` の `paths` 配列の**唯一の要素の文字列値**（TOML のリテラル文字列は解釈を行わないため、引用符の内側がそのまま値になる）。**行そのものでも、正規表現としての等価性でもない** |
| d-1-c | **比較の単位**は**バイト列の完全一致**。大文字小文字の同一視・空白の除去（trim）・正規表現としての等価判定・正規化を**一切行わない**。値の前後に空白があれば（`''' ^apps/web/\.next/'''` 等）**不合格**とする。配列の角括弧の内側で要素の**外**に置かれた空白（`[ '''^apps/web/\.next/''' ]`）は TOML の記法上の空白であり値ではないため、許してよい |
| d-1-d | **禁止パターンを列挙する方式（`\|` を禁じる・`.*` を禁じる 等）で塞がない。** それは 11-9-c → 旧 12-2-d と2度繰り返した「内容側の担保をベクタの列挙へ委譲する」誤りの3度目になる。**許可する文字列を1つに固定する側で書く** |
| d-1-e | **この値を差し替えることは「許可集合の変更」であり、12-7-a のとおり人間の承認が要る。** 実装工程・レビュー工程が独断で差し替えない。差し替えるときは**本仕様（d-1-a）を先に更新**し、その値が意図した範囲であることを 11-4 / 12-4 の**意味の検証**で確かめる |

##### 12-2-d-2. `paths` 行に行末コメントを置かない（2026-08-18 決定。W-a の解消）

**仕様と実装が矛盾していた事実を残す。** 旧 12-2-a は行末コメントを一律に許可し、旧 12-3-j は「正常形の**各行**に行末コメントを付けたもの」を**合格**と定めていた。しかし実測では、`paths` 行に行末コメントを付けると既存検査の値の切り出しが失敗し、`11-4 の本番適用` と `12-4 の本番適用` の2件が **FAIL** する。**仕様どおりに書くと検査が赤くなる**状態だった。

| # | 要求 |
|---|---|
| d-2-a | **`paths` 行には行末コメントを置かない。** 置かれていた場合は**不合格**とする（12-3-r） |
| d-2-b | **`paths` 行以外**（`[extend]` / `useDefault` / `[[allowlists]]` の各行）の行末コメントは**従来どおり許す**（12-3-j） |
| d-2-c | **寄せ先を「許さない」側にした理由。** 完全一致（d-1）の比較対象は**値**であり、`paths` 行から値を一意に切り出せることが前提になる。行末コメントを許すと切り出しに構文解析が要り、**引用符内の `#` とコメント開始の `#` を取り違える**という新しい抜けの系統（12-2-f が既に警告している形）を作る。これは「内容側の担保を曖昧にする」という C-3 と同じ方向であり、**便宜のために攻撃面を広げることになる**。`.gitleaks.toml` に理由を書く必要は**行頭コメント**で満たせる（11-10-c が求めるのは「理由1〜2行と本仕様へのリンク」であり、行末コメントを要求していない） |
| d-2-d | **これは許可集合を「狭める」変更**であり、12-7-a が人間の承認を要すると定める「足す」方向ではない。**実装（値の切り出し）を直させる選択は採らない** |
| d-2-e | 違反時のメッセージは**当該行（`paths` 行）と、行末コメントが許可外である旨**を出す（12-6-c） |

#### 12-3. 必須テストベクタ（fixture → 期待）

**「合格」= 検査が違反を1件も報告しない。「不合格」= 検査が違反を報告し、終了コードが非0になる。**

| # | fixture（`.gitleaks.toml` の内容） | 期待 | 根拠 |
|---|---|---|---|
| 3-a | 現行の正常形（`[extend] useDefault = true` + `[[allowlists]] paths = ['''^apps/web/\.next/''']` のみ） | **合格** | 12-2 |
| 3-b | リポジトリ直下の**実ファイル** `.gitleaks.toml` | **合格** | 回帰 |
| 3-c | 正常形の `[extend]` 内に `disabledRules = ["generic-api-key"]` を**1行足した**もの（**新しい `[extend]` テーブルを作らない**） | **不合格** | 12-2-c。**現行検査が素通りする形**。実害は全域の沈黙 |
| 3-d | 正常形に **2本目の `[[allowlists]]`** を足したもの | **不合格** | 12-2-b。現行検査が素通りする形 |
| 3-e | 正常形に `[[rules]]`（既存ルールと同じ `id` の上書きを含む）を足したもの | **不合格** | 12-2-b |
| 3-f | 正常形に `[allowlist]`（**単数形・非配列**）を足したもの | **不合格** | 12-2-b。別スキーマでの allowlist 追加 |
| 3-g | 正常形に `[[rules]]` + `[[rules.allowlists]]`（ルール個別の迂回）を足したもの | **不合格** | 12-2-b |
| 3-h | 正常形の `[[allowlists]]` に `regexes` または `stopwords` を足したもの | **不合格** | 12-2-d |
| 3-i | 正常形に**トップレベルのドット付きキー** `extend.disabledRules = ["generic-api-key"]` を足したもの | **不合格** | 12-2-e。記法違いの抜けを塞ぐ |
| 3-j | 正常形の **`paths` 行以外**の各行に**行末コメント**を付けたもの（例: `useDefault = true  # 理由`） | **合格** | 12-2-a / 2-d-2-b / 2-f。誤検知しないこと。**2026-08-18 訂正**（旧: 「各行に」。`paths` 行を含めると実装が FAIL し、仕様と実装が矛盾していた。12-2-d-2） |
| 3-k | リポジトリ直下に `.gitleaksignore` が存在する状態 | **不合格** | 12-2-g |

##### 12-3 の追加ベクタ（2026-08-18。完全一致・不在・行末コメントの検証）

**期待値はいずれも「不合格」である。** 3-l は往復4で **26/26 通過（素通り）が実測された**形、3-m は**同型だが未実測**の形（実測の有無で必須度を変えない。12-2-d-1-d のとおり値を数え上げないため、同型は代表ベクタで足りる）、3-n は**往復4の M1 の回帰**である。

| # | fixture（`.gitleaks.toml` の内容） | 期待 | 根拠 |
|---|---|---|---|
| 3-l | 正常形の `paths` の値を **`^apps/web/\.next/\|^infra/prod`**（選択肢の追加）に置換 | **不合格** | 12-2-d-1-a。**現行検査が素通りする形（C-3）**。対照実験で `leaks found: 2` → `1`。`\|^\.github/` / `\|\.env` も**同型**であり、本ベクタが代表する（禁止パターンを列挙しないため、選択肢の中身を数え上げない。12-2-d-1-d） |
| 3-m | 正常形の `paths` の値を **`^apps/web/\.next/.*`**（末尾の緩和）に置換 | **不合格** | 12-2-d-1-a。11-4 の11本の判定を**変えない**ため、ベクタ列挙型の検査では捕捉できない形 |
| 3-n | 正常形の `paths` の値を **`apps/web/\.next/`**（`^` 欠落）に置換 | **不合格** | 12-2-d-1-a。**往復4の M1 の回帰**。12-4-d が要求する「`^` を削った fixture」と**同一のもの**であり、両者を別々に用意しない |
| 3-o | 正常形から **`[extend]` ブロックごと削除**したもの（`[[allowlists]]` のみ） | **不合格** | 12-2-b。**W-b**。既定ルールが全滅して何も検出しないのに緑になる形（11-2 の最悪の失敗モード） |
| 3-p | `[extend]` は残し、**`useDefault = true` の行だけを削除**したもの | **不合格** | 12-2-c。3-o と同じ実害に別経路で到達する形 |
| 3-q | 正常形から **`[[allowlists]]` ブロックごと削除**したもの（`[extend]` のみ） | **不合格** | 12-2-b。実害の向きは逆（検査が厳しくなる側）だが、**除外範囲の変更は人間の決定事項**であり機械が黙って通さない（2-b 補足） |
| 3-r | 正常形の **`paths` 行に行末コメント**を付けたもの（例: `paths = ['''^apps/web/\.next/''']  # 理由`） | **不合格** | 12-2-d-2-a。**W-a**。旧 3-j はこれを合格としており、仕様と実装が矛盾していた |
| 3-s | 正常形の `paths` の値の**前後に空白**を入れたもの（例: `''' ^apps/web/\.next/'''`） | **不合格** | 12-2-d-1-c。**比較の単位が「trim 後」ではなくバイト列であること**を確かめる対照 |
| 3-t | 正常形の各行に**行頭のインデント**を付けたもの | **不合格** | 12-2-h。記法違いの抜けを塞ぐ（12-2-e と同じ趣旨） |

#### 12-4. `paths` の正規表現に先頭アンカーがあること

**本節は 2026-08-18 に完全一致（12-2-d-1）を導入した後も存置する。縮小も削除もしない。** 理由は 4-f。

| # | 要求 |
|---|---|
| 4-a | **`paths` の正規表現が `^` による先頭アンカーを持つことを検査する。** `apps/web/\.next/` というプレフィックス文字列が保持されていても、**アンカーが無ければ不合格**とする。<br>**（2026-08-18 追記）この要求は 12-2-d-1 の完全一致に厳密に包含される**（アンカーを欠いた値は既定値と完全一致しないため、12-2-d だけでも不合格になる）。**包含されることは要求を弱める根拠にならない**（4-f）。本項は引き続き有効であり、**アンカーの欠落は 12-2-d と 12-4-a の両方で不合格**になる |
| 4-b | 判定はアンカーの**有無を静的に見るだけで済ませない。** 次の**位置ずれベクタ**をパス判定に加える（11-4 の表は**変更しない**。11-4 のどのパスも位置ずれを含まないため、`^` を削っても 11-4 は全件通る = これが素通りの原因だった） |
| 4-c | 位置ずれベクタ: `vendor/apps/web/.next/prerender-manifest.json` → **不一致**／`x/apps/web/.next/a.json` → **不一致**／`../apps/web/.next/a.json` → **不一致**／`apps/web/.next/prerender-manifest.json` → **一致**（対照） |
| 4-d | **`^` を削った fixture が 4-c で不合格になること**を、正常形が合格することと**対で**確かめる（片方だけでは検査が効いているか分からない。11-6-a と同じ設計） |
| 4-e | **11-3-f との衝突は停止条件。** 11-3-f はパスが絶対で評価されると**実測された場合に限り** `(^|/)apps/web/\.next/` を許すが、この形は 4-c の `vendor/apps/web/.next/...` に**一致してしまい**、4-c と両立しない。現行実装は `^apps/web/\.next/` で 11-4 / 11-6 とも緑であり、**相対パスで評価されることが実証済み**なので 11-3-f は発動していない。将来 11-3-f を発動させる必要が生じた場合は、**どちらを優先するかを推測で決めず、AC-8-3 / 11-9-a に従って停止し人間へ上げる** |
| 4-e-2 | **（2026-08-18 追記）完全一致（12-2-d-1）を入れた後の 11-3-f の扱い。** 許可値が `^apps/web/\.next/` の1つに固定されたため、**`(^\|/)apps/web/\.next/` は 12-2-d-1-a に照らして不合格**になる。すなわち 11-3-f は「実装工程が実測を根拠に独断で採ってよい選択肢」**ではなくなり**、発動には **12-2-d-1-a の許可値の差し替え = 12-7-a の人間の承認**が要る。<br>**これは 11-3-f を弱めるのではなく、発動経路を人間の承認に一本化する強化である。** 4-e が定めた停止条件（推測で決めず人間へ上げる）は、これまで規律だけで守られていたが、完全一致によって**機械が強制する**（AI が黙って書き換えると検査が赤くなる）。11-3-f の「それ以外の緩め方をしない」は据え置く |
| 4-f | **（2026-08-18 追記）12-4 を削除・縮小しない理由。** 4-a が 12-2-d-1 に包含されることは事実だが、次の3点は完全一致では代替できない。<br>**(1) 意味の検証は完全一致にはできない。** 完全一致が担保するのは「値が固定の1つであること」だけで、「**その1つが意図した範囲を表しているか**」は判定しない。許可値を人間が差し替えるとき（12-2-d-1-e）に、新しい値が正しい範囲かを実際の正規表現エンジンで確かめるのは 4-c の位置ずれベクタと 11-4 の表**だけ**である。ここを消すと、差し替え時に**根拠が消える**。<br>**(2) 4-e / 4-e-2 の停止条件が本節に載っている。** 節ごと消すと 11-3-f との衝突の整理と停止条件が失われる（**停止条件の弱体化にあたるため、削除は不可**）。<br>**(3) 対で確かめる設計（4-d）は多層防御として残す。** 完全一致の検査コード自体が壊れた・値の切り出しに失敗した場合に、位置ずれベクタが独立に赤くなる。<br>**ただし本節を「捕捉の主たる根拠」に格上げしない。** 捕捉の根拠は 12-2 のホワイトリスト（12-2-d-1 を含む）にあり、**ベクタを増やすことでホワイトリストを緩める代償にしない**（12-L4 と同じ） |

#### 12-5. TOML のパースエラーを捕捉の根拠にしない

| # | 要求 |
|---|---|
| 5-a | 合否は **`.gitleaks.toml` の記述に対する検査**で決める。「gitleaks が設定を読めずに落ちた」ことを**捕捉の根拠にしない** |
| 5-b | 具体例（実測に基づく申し送り）: `disabledRules` を**別の `[extend]` テーブル**として追記すると、TOML のテーブル重複エラーで**設定の読み込み自体が失敗**して検査は落ちる。しかしそれは**検査が捕捉したのではなく TOML パーサが落ちているだけ**である。**既存の `[extend]` テーブル内に1行書かれた場合**（3-c。TOML として妥当であり、現行検査を素通りする形）を捕捉できることを必須とする |
| 5-c | 12-3 の**不合格 fixture は原則として TOML として妥当な形で書く。** パースエラーで落ちる形を捕捉の証拠に使わない |

#### 12-6. 置き場所・実行

| # | 要求 |
|---|---|
| 6-a | **新しい make ターゲットを増やさない**（11-6-e / AC-4-6 と同じ理由）。既存の `.github/scripts/test-gitleaks-allowlist.sh`（`make test-scripts` から呼ばれる）へ合流させる |
| 6-b | 本 AC の検査は**静的検査**であり、**11-6 の動的検査（`useDefault` が実際に効いているかの対）を置き換えない。** 両方が走ること（静的だけでは綴り・配置が効いているか分からない = 11-6-d、動的だけでは 12-3 の3経路を素通りする = 11-9-c） |
| 6-c | 違反時のメッセージには**どの行のどの記述が許可外か**を出す（弱体化の再発時に、何を戻せばよいかが分かること） |

#### 12-7. 許可集合を広げるには人間の承認が要る

| # | 要求 |
|---|---|
| 7-a | **12-2 の許可集合に項目を足すことは「検査の弱体化」**であり、**11-9-a と同じ扱い**とする。AI が独断で行わない。「設定に1キー足したいから許可集合も足す」は、弱体化と改善の区別がつかない典型である |
| 7-b | 本 AC は **gitleaks 8.30.1** の設定スキーマに依存する（11-10-a と同じ）。版を上げるときは 11-10-b に従い、**12-3 のベクタも実測し直す** |

#### AC-12 の限界 — この検査が意味しないこと

| # | 内容 |
|---|---|
| 12-L1 | **「将来広げられないこと」は保証しない。** 検査コードや 12-3 のベクタごと書き換える変更は機械では止まらない（11-L5 と同じ。11-9-a / 12-7-a の規律が最終防衛線） |
| 12-L2 | 検査対象は **`.gitleaks.toml` の記述と `.gitleaksignore` の不在**だけである。**それ以外の経路からの弱体化は見ない。** 例: `make scan-secrets` の引数変更（`--config` の差し替え・`--no-git` 等の付与）、CI ワークフロー側での検査のスキップ、ソース中の `gitleaks:allow` インラインコメント、環境変数による設定 |
| 12-L3 | 本 AC は**除外の範囲が正しいこと**を担保しない。範囲は 11-3 / 11-4 が見る。本 AC が見るのは「**範囲を定める記述以外のものが混ざっていないこと**」である |
| 12-L4 | 12-3 の不合格 fixture が捕捉されることは、**未知の弱体化が捕捉されること**を意味しない。捕捉の根拠はベクタの列挙ではなく **12-2 のホワイトリスト**にある。**ベクタを増やすことでホワイトリストを緩める代償にしない** |
| 12-L5 | **（2026-08-18 追加）完全一致（12-2-d-1）にしても残る抜け道。過小に書かない。** 完全一致が終わらせたのは「**`.gitleaks.toml` の `paths` の値を書き換えるだけで除外範囲を広げられる**」という系統**だけ**である。以下はいずれも**残っている**。<br>(1) **許可値そのものの差し替え。** 12-2-d-1-a の値と検査コード側の期待値を**同時に**書き換えれば緑になる。「その1つが何であるべきか」の正解は本仕様（`docs/`）にしかなく、担保は 12-7-a の規律と**差分レビュー**だけである（12-L1 と同じ）。<br>(2) **検査コード・fixture ごとの書き換え**（12-L1）。<br>(3) **`.gitleaks.toml` の外からの弱体化**（12-L2 の全項が有効）: `make scan-secrets` の**引数変更**（`--config` の差し替え・`--no-git` 等の付与）、**CI ワークフロー側での検査のスキップ**、ソース中の **`gitleaks:allow` インラインコメント**、**環境変数**による設定。**これらは完全一致では1件も止まらない。**<br>(4) **`.gitignore` の連動**（11-L2）。除外の許容根拠である「コミット経路が無いこと」が崩れても、この検査は赤くならない。<br>(5) **検出能力そのものの限界**（11-L4）。gitleaks はパターン検出であり、未知の形式・伏せられた値は捕まえない |
| 12-L6 | **（2026-08-18 追加）完全一致は「範囲が正しいこと」を担保しない。** 見ているのは「値が固定の1つであること」だけで、**その1つが意図した除外範囲を表しているか**は判定しない（12-L3 と同じ切り分け）。範囲の意味は 11-4 の表と 12-4-c の位置ずれベクタが見る。**したがって 11-4 / 12-4 を「冗長だから」と削らない**（11-4 表下の注記 / 12-4-f） |

---

## U-1. レート制限のパラメータ — **決定済み（2026-08-14 / 案1）**

**決定**: 本 Issue #9 から `src/lib/rate-limit.ts` を**外す**。レート制限のパラメータ（下記 a〜e）は、**最初の Route Handler を作る Issue でセットで決める**。#9 はスキャフォールドとハーネスの SKIP 解消に限定する。

| 項目 | 内容 |
|---|---|
| 決定日 | 2026-08-14 |
| 決定者 | 人間（AI の仮置きではない） |
| 採った案 | **案1**（下表） |
| #9 への影響 | **完了条件は変わらない**（`make lint-web` / `make test-web` が SKIP でなく走る・CI の `web` ジョブが実際に検査する・`make verify` が通る）。レート制限は元から完了条件に含まれていない |
| 実装への拘束 | **素通しの実装・TODO スタブ・既定値の仮置きを置かない**（AC-8-5）。「ガードレールがあるように見えて効いていない」状態を作らない |
| 引き継ぎ先 | 最初の Route Handler を作る Issue。着手条件は AC-9 に固定済み（案1の決定後も外れない） |

### なぜ #9 で決めなかったか（記録）

以下は決定前の争点であり、**削除せず残す**。次の Issue が a〜e を決めるための入力である。

| 根拠 | 内容 |
|---|---|
| `docs/adr/0010-harness-engineering.md` §A | 「改変してはならない（人間の承認が要る）」に **検査の閾値**と**コストガードレール**が列挙されている。列挙されていない改変は既定で禁止 |
| `docs/rules/responsibility.md` | 業務ルール・ガードレールは「必ず人間に確認を取る」側。判断に迷ったら実装を進めず**質問して止まる** |
| `docs/ai-collaboration.md`「AIの停止条件」 | 仕様に書かれていない判断が必要になったら停止する。**推測で埋めることを禁止する** |
| `docs/rules/cost-guardrails.md` | 「Route Handler | レート制限」とあるだけで、**上限・ウィンドウ・キー・超過時の応答のいずれも決まっていない** |

### 決めるべき項目（**次の Issue へ持ち越し。a〜e はセットで決める**）

**以下はいずれも未決のままである。** 案1は「#9 で決めない」ことを決めたのであって、a〜e の中身を決めたのではない。**部分的に先取りして実装しない。**

| # | 項目 | なぜ AI が決められないか | 選択肢の例 |
|---|---|---|---|
| a | **キーの取り方** | 誰を単位に絞るかで防御対象が変わる。CloudFront 経由のため `X-Forwarded-For` の左端は詐称可能 | (1) クライアント IP（`CloudFront-Viewer-Address` を信頼する）／(2) セッション・Cognito `sub`（**ゲスト閲覧は未認証なので効かない**）／(3) キー無し（インスタンス単位の総量） |
| b | **上限リクエスト数とウィンドウ** | 数値そのものが「検査の閾値」であり、正規のデモ利用を 429 にする境界を決める | 例: N req / M 秒 / キー。**値は人間が決める** |
| c | **カウンタの置き場所** | Lambda はインスタンスごとにメモリが分かれる。予約同時実行数 5（`docs/rules/cost-guardrails.md`）のため、**インメモリだと実効上限が最大5倍になる** | (1) インメモリ（近似。5倍を受け入れる）／(2) 外部ストア（DynamoDB 等。**新規リソースはコストと使用禁止リストの再検討が要る**） |
| d | **超過時の応答** | 挙動が利用者に見える | 例: 429 + `Retry-After`。ADR 0013 は同時実行超過による 429 を「意図的な挙動でありバグではない」としており、整合を取る必要がある |
| e | **適用範囲** | CloudFront 帯域の一次防御として効かせるなら SSR ページも対象になるが、**画面ロード自体が 429 になる** | (1) Route Handler のみ／(2) SSR ページを含む全リクエスト |

### 提示した選択肢と、採った案（記録）

| 案 | 内容 | 帰結 | 判定 |
|---|---|---|---|
| **案1** | **本 Issue から `src/lib/rate-limit.ts` を外し、最初の Route Handler を作る Issue で a〜e とセットで決める。** 本 Issue はスキャフォールドとハーネスの SKIP 解消に限定する | Issue #9 の「やること」を1つ減らす判断。**Issue の完了条件（SKIP 解消 / CI / `make verify`）はレート制限を含まないため、完了判定は変わらない。** レート制限の対象（Route Handler）が本 Issue に存在しないことと整合する | **採用（2026-08-14）** |
| **案2** | いま a〜e を人間が決め、本仕様に AC-11 として追記してから tester 工程へ渡す | 本 Issue の中で実装できる。ただしレート制限の対象（Route Handler）が無い状態で関数だけが入るため、**実際に効いているかを本 Issue では検証できない**（AC-3-3 の「対象が壊れても落ちないテスト」に近づく） | 採らない |
| **案3** | AI が既定値を仮置きし、後で調整する | ADR 0010 §A に反する。かつ「ガードレールがあるように見えて効いていない」状態を作り、CLAUDE.md の「コストとセキュリティのガードレールは機能要件と同格」に反する | 採らない |

**決定の正解はこの仕様にある。** Issue コメント・PR コメントに書き写して終わらせない（ADR 0004）。**a〜e が決まるまで、tester / implementer は rate-limit に着手しない**（AC-8-5 / AC-9）。

---

## 関連

- `docs/harness/verification-loop.md`: SKIP の扱い／対象ゼロを成功にしない／必須チェックへの登録／ハーネスの性質（AC-4 / AC-5 / AC-6 / AC-7）
- `docs/rules/development-process.md`: SDD と `no-spec` の位置づけ（本仕様を起こす根拠）／Web は Vitest
- `docs/rules/commands.md`: `make verify` の中身、版の固定箇所（P-2 / AC-6-5）
- `docs/rules/architecture.md`: BFF 経由の一方通行、`apps/web/src/lib/lambda-client.ts`（AC-1-6 / AC-9）
- `docs/rules/cost-guardrails.md`: Route Handler のレート制限（U-1。本 Issue から外す決定 = 2026-08-14 / 案1）
- `docs/rules/security.md`: `.env` と機密ファイルの扱い（AC-8-1）
- `docs/adr/0007-testing-with-stdlib-and-go-cmp.md` §5: Vitest の選定と #9 への委譲（P-5 / AC-3）
- `docs/adr/0010-harness-engineering.md` §A: 独断してよい範囲／承認が要る範囲（U-1 / AC-8-3 / AC-11-9a）。§B: `docs/rules/` の肥大化（AC-7-5 / AC-11-10c）
- `docs/harness/verification-loop.md`: `make scan-secrets`（作業ツリー + 履歴）の位置づけと CI の `secrets` ジョブ（AC-11 / AC-12）。**gitleaks の除外範囲の正解は本仕様にある**（ADR 0004）
- `docs/adr/0013-aws-native-hosting-over-vercel.md`: OpenNext、CloudFront の従量リスク、429 の扱い（P-6 / AC-1-8 / U-1）
- `docs/adr/0014-execution-role-over-vercel-oidc.md` / `0003`: SigV4 は実行ロール（AC-9）
- `docs/adr/0015-claude-design-for-design-system.md`: 配置先を「スキャフォールド時に確定する」と本仕様へ委譲。**その委譲は AC-1-5（`apps/web/src/components/`。2026-08-14 に人間が承認）で解消済み**
- `docs/adr/0016-cognito-end-user-authentication.md`: 認証は BFF で終端（AC-9）
- `docs/specs/devcontainer-go-module-policy.md`: allowlist の非スコープ、npm への波及（P-3 / AC-10-4）
- `docs/specs/readme-adr-sync.md`: 同期を口実に未完了を隠さない（AC-7-4）
- `docs/specs/work-month-screen-ui.md` ほか UI 仕様: 本 Issue の非スコープ（AC-9）
